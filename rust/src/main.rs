
#[derive(Debug)]
struct Action {
}

fn get_valid_actions(p0: (), p1: _) -> _ {
    todo!()
}
#[cfg(test)]
mod tests {
    use std::collections::HashSet;
    use crate::{get_valid_actions, Action};

    #[test]
    fn test_move_is_valid() {
        let prison = ();
        let node2 = ();
        let state = ();

        assert_eq!(get_valid_actions(prison, vec!["banana"]), vec![Action {}]);
    }

}

fn main() {}
